const Loading = () => {
    return (
        <div className="flex justify-center items-center w-full h-full">
            <span className="flex w-10 h-10 bg-[blue] rounded-full animate-pulse"></span>
        </div>
    );
};

export default Loading;
